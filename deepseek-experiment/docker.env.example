# Trading Bot Configuration
TRADING_MODE=paper
USE_TESTNET=true
LLM_PROVIDER=mock
LLM_API_KEY=
LLM_MODEL=

# Exchange Configuration
EXCHANGE=kraken  # Options: kraken, bybit, binance, coinbase
# Note: Kraken does not have a testnet/sandbox environment like Bybit/Binance
# USE_TESTNET flag has limited effect with Kraken - paper trading is controlled by TRADING_MODE
SYMBOL=BTC/USDT  # Note: Kraken may use BTC/USD format - try BTC/USD if BTC/USDT doesn't work

# Trading Parameters
INITIAL_BALANCE=10000.0
MAX_POSITION_SIZE=0.1
STOP_LOSS_PERCENT=2.0
TAKE_PROFIT_PERCENT=3.0
RUN_INTERVAL_SECONDS=300

# Logging
LOG_LEVEL=INFO

# Exchange API Keys
# For paper trading: Optional - may be needed for fetching real market data
# For live trading: Required - must have valid Kraken API credentials
# Note: Kraken does not provide separate testnet keys
EXCHANGE_API_KEY=your_kraken_api_key
EXCHANGE_API_SECRET=your_kraken_api_secret
# TESTNET_API_KEY and TESTNET_API_SECRET are not used with Kraken (no testnet available)

# Database Configuration (Supabase)
# Required for API server to persist data
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your_supabase_anon_key
# SUPABASE_SERVICE_KEY=your_supabase_service_key  # Optional: for admin operations
# DATABASE_URL=postgresql://postgres.xxx:password@db.xxx.supabase.co:5432/postgres  # Optional: for Alembic migrations

# API Server Configuration
# ENVIRONMENT=production
# CORS_ORIGINS=http://localhost:3000,https://your-frontend-domain.com
