# Frontend Environment Configuration Template
# Copy this file to .env and fill in your values

# =============================================================================
# API CONFIGURATION
# =============================================================================
# API base URL - Leave empty to use relative paths (works with Vercel rewrites)
# Set this for direct API access bypassing proxies
# Examples:
#   - Local development: http://localhost:8001
#   - Railway production: https://your-api.railway.app
#   - Leave empty: Uses relative paths (/api/*)
VITE_API_URL=

# =============================================================================
# DEVELOPMENT CONFIGURATION (docker-compose.dev.yml only)
# =============================================================================
# Docker environment flag (set automatically by Docker Compose)
DOCKER_ENV=false

# API proxy target for Vite dev server (Docker Compose uses service names)
VITE_API_PROXY_TARGET=http://localhost:8001

# WebSocket proxy target for Vite dev server
VITE_WS_PROXY_TARGET=ws://localhost:8002
