# API Server Dependencies
# This file includes all dependencies needed for the API server
# When Railway root is set to web-dashboard, this file is used

# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
python-multipart==0.0.6

# Database - Using known compatible versions to avoid proxy argument error
# These versions are known to work together
# Let supabase manage its sub-dependencies to avoid conflicts
supabase==2.8.1
gotrue==2.8.1
httpx==0.24.1
# postgrest, storage3, realtime will be installed automatically by supabase

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0

# Shared dependencies (from parent requirements.txt)
# These are needed because api_server_supabase.py imports from parent (config, supabase_client)
requests>=2.31.0
psutil>=5.9.0
rich>=13.0.0
colorama>=0.4.6
