# API Server Dependencies
# This file includes all dependencies needed for the API server
# When Railway root is set to web-dashboard, this file is used

# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets>=13.0  # Latest version compatible with Python 3.13
python-multipart==0.0.6

# Database - Using latest versions compatible with Python 3.13
# We don't use realtime features, so we can use newer versions
supabase>=2.9.0
# Let supabase manage its sub-dependencies (gotrue, httpx, postgrest, storage3, realtime)

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0

# Shared dependencies (from parent requirements.txt)
# These are needed because api_server_supabase.py imports from parent (config, supabase_client)
requests>=2.31.0
psutil>=5.9.0
rich>=13.0.0
colorama>=0.4.6
